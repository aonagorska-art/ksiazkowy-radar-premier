import type { Book } from '../data/books'
import { BookCard } from './BookCard'
import { ArrowRight } from 'lucide-react'
export function UpcomingReleases({books,onOpen,favorites,onToggleFavorite}:{books:Book[];onOpen:(book:Book)=>void;favorites:string[];onToggleFavorite:(id:string)=>void}) { const featured:Book[]=[];const used=new Set<string>();for(const book of books){if(!used.has(book.publisher)){featured.push(book);used.add(book.publisher)}if(featured.length===4)break}for(const book of books){if(featured.length===4)break;if(!featured.includes(book))featured.push(book)}return <section className="upcoming section-wrap" id="premiery"><div className="section-heading"><div><p className="eyebrow">Na horyzoncie</p><h2>Najbliższe premiery</h2></div><a href="#kalendarz" className="text-link">Pełny kalendarz <ArrowRight size={18}/></a></div><div className="upcoming-grid">{featured.map(book=><BookCard key={book.id} book={book} onOpen={onOpen} isFavorite={favorites.includes(book.id)} onToggleFavorite={onToggleFavorite}/>)}</div></section> }
