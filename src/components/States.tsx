import { Radio, RotateCw } from 'lucide-react'
export function EmptyState({type}:{type:'month'|'search'|'filter'}) { const messages={month:['Radar chwilowo niczego nie wykrył 📡','Nie mamy zweryfikowanych pozycji w wybranym miesiącu.'],search:['Nie znaleźliśmy takiej książki.','Sprawdź tytuł lub autora.'],filter:['Nic nie wpadło w radar.','Spróbuj zmienić filtry.']};return <div className="empty-state"><Radio size={28}/><h3>{messages[type][0]}</h3><p>{messages[type][1]}</p></div> }
export function ErrorState({onRetry}:{onRetry:()=>void}) {return <div className="empty-state"><Radio size={28}/><h3>Radar ma chwilową awarię.</h3><p>Nie udało się załadować premier.</p><button className="secondary-button" onClick={onRetry}><RotateCw size={16}/> Spróbuj ponownie</button></div>}
export function Skeleton({variant}:{variant:'cards'|'calendar'|'modal'}) {return <div className={`skeleton skeleton-${variant}`} aria-label="Ładowanie premier" role="status">{Array.from({length:variant==='cards'?4:variant==='calendar'?35:2},(_,i)=><span key={i}/>)}</div>}
