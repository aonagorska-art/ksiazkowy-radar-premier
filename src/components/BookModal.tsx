import { useEffect, useRef } from 'react'
import { Heart, X, ArrowUpRight } from 'lucide-react'
import type { Book } from '../data/books'
import { formatDate } from '../lib/date'
import { empikSearchUrl } from '../lib/links'
export function BookModal({book,onClose,isFavorite,onToggleFavorite}:{book:Book|null;onClose:()=>void;isFavorite:boolean;onToggleFavorite:(id:string)=>void}) { const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!book)return;const previous=document.activeElement as HTMLElement|null;const oldOverflow=document.body.style.overflow;document.body.style.overflow='hidden';ref.current?.querySelector<HTMLElement>('button')?.focus();function handleKey(e:KeyboardEvent){if(e.key==='Escape')onClose();if(e.key==='Tab'){const elements=Array.from(ref.current?.querySelectorAll<HTMLElement>('button,a[href]')??[]);const first=elements[0],last=elements.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last?.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first?.focus()}}}document.addEventListener('keydown',handleKey);return()=>{document.removeEventListener('keydown',handleKey);document.body.style.overflow=oldOverflow;previous?.focus()}},[book,onClose]);if(!book)return null;return <div className="modal-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div className="book-modal" role="dialog" aria-modal="true" aria-labelledby="modal-title" ref={ref}><button className="modal-close" onClick={onClose} aria-label="Zamknij szczegóły"><X/></button><div className="modal-cover"><img src={book.cover} alt={`Okładka książki ${book.title}`}/></div><div className="modal-content"><span className="eyebrow">Premiera · {formatDate(book.releaseDate)}</span><h2 id="modal-title">{book.title}</h2><p className="modal-author">{book.author}</p><div className="modal-details"><div><span>Wydawnictwo</span><strong>{book.publisher}</strong></div><div><span>Gatunek</span><strong>{book.genre}</strong></div>{book.series&&<div><span>Seria</span><strong>{book.series}, tom {book.volume}</strong></div>}</div><h3>O książce</h3><p>{book.description}</p><div className="modal-actions"><a href={book.publisherUrl} target="_blank" rel="noopener noreferrer" className="primary-button">Strona wydawnictwa <ArrowUpRight size={18}/></a><a href={empikSearchUrl(book.title,book.author)} target="_blank" rel="noopener noreferrer" className="empik-button">Szukaj w Empiku <ArrowUpRight size={18}/></a><button className={`modal-heart ${isFavorite?'active':''}`} onClick={()=>onToggleFavorite(book.id)} aria-pressed={isFavorite}><Heart size={19} fill={isFavorite?'currentColor':'none'}/>{isFavorite?'Ulubione':'Dodaj do ulubionych'}</button></div></div></div></div> }
